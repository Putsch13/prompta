{
  "id": "invalid-action-no-params",
  "expectValid": true,
  "manifest": {
    "kind": "agent",
    "secrets": ["openai"],
    "connectors": ["slack"],
    "steps": [
      {
        "type": "action",
        "connector": "slack",
        "action": "slack.send",
        "params": {}
      }
    ]
  }
}
