{
  "id": "invalid-gmail-no-to",
  "expectValid": true,
  "manifest": {
    "kind": "agent",
    "secrets": ["openai"],
    "connectors": ["gmail"],
    "steps": [
      {
        "type": "action",
        "connector": "gmail",
        "action": "gmail.send",
        "params": {
          "subject": "{{subject}}",
          "body": "{{body}}"
        }
      }
    ]
  }
}
